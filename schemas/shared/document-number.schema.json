{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "document-number.schema.json",
  "title": "DocumentNumber",
  "$comment": "Sprint 1B: 'type' é enum estrutural. Nenhum algoritmo de dígito verificador (CPF/CNPJ) validado aqui — ver src/domain/shared/document_number.py.",
  "type": "object",
  "properties": {
    "type": { "type": "string", "enum": ["CPF", "CNPJ", "PASSPORT", "RG", "OTHER"] },
    "value": { "type": "string", "minLength": 1 }
  },
  "required": ["type", "value"]
}
