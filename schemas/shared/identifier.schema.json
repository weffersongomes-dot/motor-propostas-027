{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "identifier.schema.json",
  "title": "Identifier",
  "$comment": "Sprint 1B: 'value' obrigatório e não vazio (validação estrutural, não de formato — ver src/domain/shared/identifier.py).",
  "type": "object",
  "properties": {
    "value": { "type": "string", "minLength": 1 }
  },
  "required": ["value"]
}
