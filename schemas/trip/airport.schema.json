{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "airport.schema.json",
  "title": "Airport",
  "$comment": "Sprint 1B: 'type' é enum estrutural (doméstico/internacional). Normalização de código/nome continua fora do escopo do domínio. Corresponde a src/domain/trip/airport.py.",
  "type": "object",
  "properties": {
    "code": { "type": "string", "minLength": 1 },
    "name": { "type": "string", "minLength": 1 },
    "city": { "type": "string", "minLength": 1 },
    "type": { "type": "string", "enum": ["DOMESTIC", "INTERNATIONAL"] }
  },
  "required": ["code", "name", "city", "type"]
}
